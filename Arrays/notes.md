### Arrays

Array is a collection of similar data types stored in contiguous memory locations. It is a data structure that can hold a fixed number of values of the same type. The size of an array is determined at the time of declaration and cannot be changed during runtime.

